(function( $ ){
    var methods = {
        init: function(){
            
        }

    }

    $.fn.butterfly = function() {
        return methods.init.apply( this, arguments );
    };
})( jQuery );
